name: bgp_config
topology:
  nodes:
  {% set ns_mgmt_ip_counter = namespace(mgmt_ip_counter=1) %}
  {% for router in routers %}
    {{ router.name }}:
      kind: arista_ceos
      image: ceos:4.33.0F
      mgmt_ipv4: 172.20.20.{{ ns_mgmt_ip_counter.mgmt_ip_counter }}
      startup-config: {{ router.name }}.cfg
    {% set ns_mgmt_ip_counter.mgmt_ip_counter = ns_mgmt_ip_counter.mgmt_ip_counter + 1 %}
  {% endfor %}

  {% for host in hosts %}
    {{ host.name }}:
      kind: linux
      image: alpine:latest
      mgmt_ipv4: 172.20.20.{{ ns_mgmt_ip_counter.mgmt_ip_counter }}
      network-mode: none
      exec:
        {% for iface in host.interfaces %}
        {% if iface.dhcp %}
        - dhclient {{ iface.name }}
        {% else %}
        - ip addr add {{ iface.ip }} dev {{ iface.name }}
        {% endif %}
        {% endfor %}
        {% if host.dhcp_enabled == false %}
        - ip route add default via {{ host.gateway }}
        {% endif %}
   {% set ns_mgmt_ip_counter.mgmt_ip_counter = ns_mgmt_ip_counter.mgmt_ip_counter + 1 %}
  {% endfor %}
  links:
  {% for router in routers -%}
    {% for iface in router.interfaces %}
    {% if iface.name != 'Loopback0' %}
    - endpoints: [{{ router.name }}:{{ iface.name }}, {{ iface.peer.name }}:{{ iface.peer.interface }}]
    {% endif %}
    {% endfor %}
  {%- endfor %}
